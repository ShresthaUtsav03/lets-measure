import 'package:flutter/material.dart';

class ImageOutput extends StatelessWidget {
  const ImageOutput({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('THis is output image'));
  }
}
